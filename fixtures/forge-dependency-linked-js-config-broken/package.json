{
  "name": "fixture-forge-linked-js-config-broken",
  "version": "1.0.0",
  "description": "Forge found in dependencies with linked JS configuration which doesn't map to a config file",
  "scripts": {},
  "dependencies": {},
  "devDependencies": {
    "@electron-forge/cli": "^6.4.2"
  },
  "config": {
    "forge": "./custom-config.js"
  }
}
