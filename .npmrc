# https://github.com/electron-userland/electron-builder/issues/8175
virtual-store-dir-max-length=80

# Hoisting patterns ensure these dev tools are installed at the root level
# This improves performance and prevents duplicate installations
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=*typescript*
public-hoist-pattern[]=*vitest*
public-hoist-pattern[]=*rollup*

# Dependency settings - needed for handling electron as an optional peer dependency
auto-install-peers=true
strict-peer-dependencies=false

# Allow warnings to be displayed (including build script warnings)
# This ensures Husky's prepare script runs automatically
loglevel=warn
